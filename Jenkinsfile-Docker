#!groovy

ansiColor('xterm') {

  node('master') {

    stage('Build') {

      checkout scm

      def DIRECTORY = params.DIRECTORY

      dir("$DIRECTORY") {
        sh 'docker-compose build'
        sh 'docker-compose push'
      }
    }
  }
}
